# README 
Drop photos here (filenames must match js/config.js):
- letter.jpg   → photo inside the opened letter
- finale.jpg   → photo on the final screen
JPG/PNG/WebP all work if you update the paths in config.
